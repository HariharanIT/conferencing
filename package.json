{
  "name": "agora-app-builder",
  "version": "1.0.0",
  "scripts": {
    "start": "app-builder-init",
    "start:info": "app-builder-init --info",
    "ci": "npm i && npm run start:info && cd conferencing && npm run web:build && mv ../vercel.json ./dist/"
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "agora-app-builder-cli": "0.0.10"
  }
}